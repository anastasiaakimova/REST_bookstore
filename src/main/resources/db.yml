jdbc:
  driverClassName: com.mysql.cj.jdbc.Driver
  url: jdbc:mysql://localhost:3306/bookmanager?serverTimezone=Europe/Minsk&useSSL=false
  username: root
  password: root

hibernate:
  dialect: org.hibernate.dialect.MySQL8Dialect
  show_sql: true