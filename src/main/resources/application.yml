server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookmanager
    username: root
    password: root
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: true
    hibernate:
      ddl-auto: update

